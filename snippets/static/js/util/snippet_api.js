define(['jquery'], function($) {
    var api_root,
        api_root_response;
    
    // get api root
    api_root_response = $.getJSON("/api/");
    // throw error if it doesn't load
    // extract json
    // for each end point
    //      create function to talk with it

});
